{
    "version": 2,
    "routes": [
        { "handle": "filesystem" },
        { "src": "/send-email", "dest": "src/api/server.js" },
        {
            "src": "/(.*)",
            "dest": "/dist/$1"
        }
    ]
}
