{
    "version": 2,
    "routes": [
        { "handle": "filesystem" },
        { "src": "/send-email", "dest": "index.js" },
        {
            "src": "/(.*)",
            "dest": "/dist/$1"
        }
    ]
}
