{
    "version": 2,
    "routes": [
        { "handle": "filesystem" },
        { "src": "/send-email", "dest": "/src/api/index.js" },
        {
            "src": "/(.*)",
            "dest": "/dist/$1"
        }
    ]
}
